type Message {
	id: ID!
	created_date: Date!
	content: String!
	sender: User!
}

type Query {
	messageById(id: ID!): Message
	messagesBySender(sender: ID!): [Message]
}

type Mutation {
	createMessage(created_date: Date!, content: String!, sender: ID!): Message
	deleteMessage(id: ID!): Message
}
